# Secrets and ports ONLY. Every tunable threshold lives in config/settings.yaml — never duplicate a value in both.
SECRET_SALT=change-me-to-a-long-random-string
ADMIN_API_KEY=change-me-to-a-long-random-string
GATEWAY_PORT=8000
DASHBOARD_PORT=8501
